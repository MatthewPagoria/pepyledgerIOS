import SwiftUI

public struct InventoryView: View {
  public init() {}

  public var body: some View {
    FeaturePlaceholderView(
      title: "Inventory",
      subtitle: "Vials, oils, pills, and projected runout surfaces."
    )
  }
}
