import SwiftUI

public struct SupportView: View {
  public init() {}

  public var body: some View {
    FeaturePlaceholderView(
      title: "Support",
      subtitle: "Ticket list, thread detail, and help escalation entry points."
    )
  }
}
