import SwiftUI

public struct CyclesView: View {
  public init() {}

  public var body: some View {
    FeaturePlaceholderView(
      title: "Cycles",
      subtitle: "Cycle schedule, titration rules, and mutation queue visibility."
    )
  }
}
